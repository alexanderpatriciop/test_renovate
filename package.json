{
  "scripts": {
    "renovate:local": "set -a; . ./.env; set +a; LOG_LEVEL=debug npx renovate --platform=local --dry-run=full",
    "renovate:github": "set -a; . ./.env; set +a; LOG_LEVEL=debug npx renovate"
  },
  "dependencies": {
    "pnpm": "^10.34.5",
    "renovate": "^44.33.2",
    "yarn": "^1.22.22"
  }
}
