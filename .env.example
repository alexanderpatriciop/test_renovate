# Renovate uses this token to authenticate with GitHub.
# Generate a new token at: https://github.com/settings/tokens/new?scopes=repo,read:org,user:email&description=Renovate+Local
# It requires `repo`, `read:org`, and `user:email` scopes.
RENOVATE_TOKEN=your_github_token_here
